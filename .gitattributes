# Shell scripts must keep LF endings — they run on the Linux server.
# Without this, a Windows checkout could convert them to CRLF and a
# subsequent scp redeploy would push a broken script.
*.sh text eol=lf
